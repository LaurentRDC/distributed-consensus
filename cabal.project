packages: raft-consensus/raft-consensus.cabal
          wal/wal.cabal

program-options
    ghc-options: +RTS -A64m -RTS

jobs: $ncpus
semaphore: True

test-show-details: streaming
tests: True
benchmarks: True
